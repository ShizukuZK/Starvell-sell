Копии файлов из `.github/workflows`. Туда их кладут `setup_github.bat` и
`release.bat`: папку `.github` удалённые инструменты записывать не могут,
поэтому обновления приходят сюда. Правьте файлы здесь.
